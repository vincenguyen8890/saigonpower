'use client'

// Savings info is now shown in the centre PlanCard (SavingsCard).
// This component is kept as a lightweight placeholder to avoid breaking imports.
export default function SavingsIndicator() {
  return null
}
